name: Release Docker image

on:
  push:
    tags:
      - "v*"

permissions:
  contents: read
  packages: write

concurrency:
  group: docker-release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  release-image:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Derive image tags
        id: image_tags
        env:
          RELEASE_TAG: ${{ github.ref_name }}
        run: |
          set -euo pipefail

          if [[ ! "$RELEASE_TAG" =~ ^v([0-9]+)\.([0-9]+)\.([0-9]+)$ ]]; then
            printf 'release tag must match vX.Y.Z, got %s\n' "$RELEASE_TAG" >&2
            exit 64
          fi

          minor_tag="${BASH_REMATCH[1]}.${BASH_REMATCH[2]}"

          {
            printf 'full=ghcr.io/zccz14/auth-mini:%s\n' "$RELEASE_TAG"
            printf 'minor=ghcr.io/zccz14/auth-mini:%s\n' "$minor_tag"
            printf 'latest=ghcr.io/zccz14/auth-mini:latest\n'
          } >>"$GITHUB_OUTPUT"

      - name: Build image and run smoke test
        env:
          IMAGE_TAG: auth-mini:release-smoke
          IMAGE_PLATFORM: linux/amd64
        run: bash docker/test-image-smoke.sh

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push GHCR image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: build/Dockerfile
          platforms: linux/amd64
          push: true
          tags: |
            ${{ steps.image_tags.outputs.full }}
            ${{ steps.image_tags.outputs.minor }}
            ${{ steps.image_tags.outputs.latest }}
